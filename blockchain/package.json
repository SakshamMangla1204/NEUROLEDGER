{
  "name": "neuroledger-blockchain",
  "version": "1.0.0",
  "private": true,
  "description": "Blockchain module for NeuroLedger",
  "type": "commonjs",
  "scripts": {
    "compile": "truffle compile",
    "migrate": "truffle migrate",
    "migrate:reset": "truffle migrate --reset",
    "test": "truffle test",
    "deploy": "truffle migrate --network development"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "truffle": "^5.11.5"
  }
}
